pipeline {
    agent any

    stages {
    stage('Test') {
      steps {
        bat '-Dliquibase.url=jdbc:mysql://localhost:3306/liquibase -Dliquibase.username=root -Dliquibase.password=root liquibase:update -DchangelogFile=db/changelog/db.changelog-sq1.sql'
      }
    }
  }
}
