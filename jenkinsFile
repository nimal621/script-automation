pipeline {
    agent any

    stages {
    stage('Test') {
      steps {
        bat 'mvn liquibase:update -Dliquibase.url=jdbc:mysql://localhost:3306/liquibase -Dliquibase.username=root -Dliquibase.password=root -DchangelogFile=db/changelog/db.changelog-sq1.sql'
      }
    }
  }
}
